diff --git a/android/build.gradle b/android/build.gradle
index 12172f8bd393d690e53ff61c67085dfe62d7fc67..374f85ad0a963ccb6f35aa40a735051420062e46 100644
--- a/android/build.gradle
+++ b/android/build.gradle
@@ -9,22 +9,17 @@ buildscript {
 	if (project == rootProject) {
 		repositories {
 			google()
-			jcenter()
+			mavenCentral()
 		}
 
 		dependencies {
-			classpath("com.android.tools.build:gradle:3.6.3")
+			classpath("com.android.tools.build:gradle:8.1.0")
 		}
 	}
 }
 
-allprojects {
-    repositories {
-        google()
-        jcenter()
-        maven { url "$projectDir/../node_modules/react-native/android" }
-    }
-}
+// jcenter() was removed here — it shut down and newer Gradle no longer ships the shorthand.
+// Everything this module needs (AndroidX, react-native) resolves from google()/mavenCentral().
 
 apply plugin: 'com.android.library'
 
@@ -36,9 +31,6 @@ android {
         targetSdkVersion safeExtGet('targetSdkVersion', 28)
         versionCode 1
         versionName "1.0"
-        ndk {
-            abiFilters "armeabi-v7a", "x86"
-        }
     }
 
     lintOptions {
@@ -49,8 +41,8 @@ android {
 dependencies {
   implementation fileTree(include: ['*.jar'], dir: 'libs')
   implementation "androidx.appcompat:appcompat:1.1.0"
-  implementation("androidx.credentials:credentials:${safeExtGet('androidCredentialsVersion', '1.3.0-alpha01')}")
-  implementation("androidx.credentials:credentials-play-services-auth:${safeExtGet('androidCredentialsVersion', '1.3.0-alpha01')}")
+  implementation("androidx.credentials:credentials:${safeExtGet('androidCredentialsVersion', '1.6.0')}")
+  implementation("androidx.credentials:credentials-play-services-auth:${safeExtGet('androidCredentialsVersion', '1.6.0')}")
   implementation "com.google.android.libraries.identity.googleid:googleid:1.1.1"
   implementation "com.facebook.react:react-native:+"
 }
